---
phase: 05-end-to-end-verification-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/service_host.py
  - tests/test_service_host.py
  - tests/test_claude_pipe_lifecycle.py
  - tests/test_process_lifetime.py
  - tests/test_startup_progress.py
  - tests/test_startup_readiness.py
autonomous: true
requirements: [VER-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The service-host descriptor-protection mechanism enforces owner-only file permissions on macOS/POSIX the same way it enforces owner-only NTFS DACLs on Windows, proven by an automated test that runs today (no Phase 1-4 dependency)."
    - "Every Windows-only test skip inventoried in .planning/codebase/TESTING.md has an explicit, source-grounded disposition — a passing macOS counterpart, a documented expected-fail-pending-Phase-2/3 counterpart, or a documented N/A-on-macOS decision — none left silently ambiguous."
    - "Windows-only tests whose macOS counterpart is blocked on an earlier phase's unfinished work (the process-tree-kill fix, the POSIX process guardian) say so in their own skip/failure reason string, not just in a planning doc nobody reads at test-run time."
  artifacts:
    - engine/service_host.py (POSIX branches added to restrict_descriptor_acl, create_protected_exclusive, verify_restricted_acl, write_descriptor)
    - tests/test_service_host.py (6 new macOS-only companion test methods)
    - tests/test_claude_pipe_lifecycle.py (2 new macOS-only companion test methods, expected-fail pending Phase 2)
  key_links:
    - "write_descriptor -> restrict_descriptor_acl / create_protected_exclusive / verify_restricted_acl: the fail-closed contract (refuse to publish the token on any unverified state) must hold identically on both platforms"
    - "test_claude_pipe_lifecycle.py macOS counterparts -> engine/backend/orgtree/codexrun.py, antigravityrun.py's still-open start_new_session gap (Plan 05-01 Task 2's pre-flight check is the automated tripwire for this same gap)"
---

<objective>
Prove VER-02: close the Windows-only test-skip gaps inventoried in `.planning/codebase/TESTING.md` and `RESEARCH.md`'s Runtime State Inventory with an explicit disposition for each one — not by silently leaving them skipped, and not by pretending a macOS counterpart is "done" when the underlying behavior doesn't exist yet.

Purpose: `.planning/codebase/TESTING.md:153` already establishes the target pattern: "When porting the equivalent POSIX permission model to macOS, write new companion tests here rather than trying to make these pass under Wine/emulation." Reading `engine/service_host.py` directly (not just the test file) shows this is not merely an untested branch: `restrict_descriptor_acl` returns `False` unconditionally on `os.name != "nt"` [VERIFIED: engine/service_host.py:134-135], and `write_descriptor` raises `OSError("descriptor protection unavailable on this platform; refusing to publish the token")` unconditionally on the same condition [VERIFIED: engine/service_host.py:231-232] — the POSIX behavior does not exist at all, so VER-02's "real coverage instead of silent skips" for this file requires implementing the missing POSIX permission model, not only writing a test around the current no-op. This module is the headless boot-host descriptor writer (`"""Headless boot host: run the engine before any interactive logon."""` [VERIFIED: engine/service_host.py:1]) — separate from Phase 1-4's stated PROC-01/02/03 (provider CLI resolution/liveness/containment) and BOOT-01/02 (launchd plist/crash-loop detection) wording, so this fix belongs to Phase 5 by elimination, not duplication.

By contrast, `test_process_lifetime.py`, `test_startup_progress.py`, `test_startup_readiness.py`, and `test_claude_pipe_lifecycle.py`'s two Windows-only tests are genuinely blocked on OTHER phases' work (the POSIX process-tree guardian, and the codexrun.py/antigravityrun.py process-tree-kill fix respectively) — this plan documents that blockage explicitly in each test's own skip/failure reason string, rather than silently leaving it implicit.

**Scope decision, recorded here per RESEARCH.md's Wave-0-gaps instruction ("capture in PLAN.md rather than leaving implicit," not a test-file edit):** `tests/installer-elevation.test.mjs` (9 gates) and the NSIS-specific `tests/disruptive/nsis-destination.test.mjs`, `installer-log.test.mjs`, `console-lifetime.test.mjs`, `lifetime-controls.test.mjs`, `boot-installer.test.ps1` have **no macOS counterpart planned** in this phase. They assert against NSIS installer macros, `powershell.exe`, and Windows registry/WMI — none of which exist on macOS, and v1 ships an unsigned `.app` with no separate installer program (PKG-01/PKG-03), so there is no macOS mechanism to test. This is contingent on assumption A1 in RESEARCH.md (Phase 1 does not introduce a signed `.pkg`/`.dmg` with its own elevation semantics) — if Phase 1 planning changes that decision, this scope call must be revisited.

Output: a real POSIX permission model in `engine/service_host.py`, 6 new macOS-only tests in `tests/test_service_host.py`, and explicit dispositions for the remaining Windows-only skips.

**Cross-phase dependency note:** unlike Plan 05-01, every task in this plan is executable and fully verifiable TODAY, on this POSIX dev machine, with zero dependency on Phase 1-4 — `engine/service_host.py` and its tests are pure Python with no Electron/packaging involvement. Task 3's two `test_claude_pipe_lifecycle.py` methods are written now but will genuinely fail (by design, documented) until Phase 2 lands the codexrun.py/antigravityrun.py fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-end-to-end-verification-test-coverage/RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@engine/service_host.py
@tests/test_service_host.py
@tests/test_claude_pipe_lifecycle.py
@engine/process_lifetime.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Real POSIX descriptor-protection in service_host.py + first macOS-only test</name>
  <files>engine/service_host.py, tests/test_service_host.py</files>
  <behavior>
    - Test: on POSIX, `service_host.restrict_descriptor_acl(target)` returns `True` (not the current unconditional `False`) and leaves `target` with mode exactly `0o600`, owned by the current uid.
    - Test: `service_host._current_user_sid()` (keep this exact name — `test_write_descriptor_fails_closed_when_protection_unavailable` patches it by name at tests/test_service_host.py:179) returns `str(os.getuid())` on POSIX instead of `None`.
    - Existing regression: `test_write_descriptor_fails_closed_when_protection_unavailable` and `test_write_descriptor_fails_closed_when_verification_fails` (tests/test_service_host.py:177-192) must keep passing unchanged — both force the "unavailable"/"unverified" path via mocking, independent of the real OS branch, so they are not testing the code this task removes.
  </behavior>
  <action>
Add a real POSIX branch to `engine/service_host.py`'s descriptor-protection chain instead of the current unconditional Windows-only gate:
- `_current_user_sid()` (line 111-120): keep the function name (tests patch it by name), add an `else` branch returning `str(os.getuid())` when `os.name != "nt"`.
- `restrict_descriptor_acl(descriptor)` (line 123-144): replace the blanket `if os.name != "nt" or not sid: return False` (line 134-135) with a per-OS branch — on POSIX, `os.chmod(descriptor, 0o600)` and return `True` (no ACL-inheritance concept to strip on POSIX; a `0600` mode already grants nothing to group/other).
- `create_protected_exclusive(path, sid)` (line 147-191): the current body is Windows-only `ctypes.WinDLL` with no platform branch at all — add a POSIX branch using `os.open(path, os.O_CREAT | os.O_EXCL | os.O_WRONLY, 0o600)`, which is atomic and race-free (matches the existing docstring's "no such instant exists" guarantee, mirroring the Windows `CREATE_NEW`+share-0 contract) — return the resulting fd directly (no `msvcrt` bridging needed on POSIX).
- `verify_restricted_acl(target)` (line 194 onward): add a POSIX branch checking `stat.S_IMODE(os.stat(target).st_mode) == 0o600 and os.stat(target).st_uid == os.getuid()`.
- `write_descriptor` (line 216-247): remove the unconditional `raise OSError("descriptor protection unavailable on this platform; refusing to publish the token")` at line 231-232 (that line stays reachable ONLY for the genuine `not sid` case — which cannot occur on a real POSIX system since `os.getuid()` always succeeds — so this line remains as a real fail-closed guard for the mocked-`None` test case, not as a platform gate). The fail-closed contract (raise-and-cleanup-temp-and-reraise) stays exactly as-is; only the `os.name != "nt"` exclusion is removed.

Add one new macOS-only test to `tests/test_service_host.py`'s `ServiceHostUnitTests` class, gated `if os.name == "nt": raise unittest.SkipTest("POSIX permissions are macOS/Linux only")` (the mirror-image skip string of the existing `"NTFS ACLs are Windows-only"` convention at line 79-80), asserting the mode-0600 + uid-match behavior above — mirroring `test_descriptor_acl_restriction_leaves_owner_system_admins_only` (line 78-95)'s structure but with `os.stat().st_mode`/`st_uid` assertions instead of the `icacls`/PowerShell `Get-Acl` check.
  </action>
  <verify>
    <automated>python -m unittest tests.test_service_host.ServiceHostUnitTests -v 2>&1 | tail -15</automated>
  </verify>
  <done>All `ServiceHostUnitTests` methods pass on this POSIX machine, including the new macOS-only test and the two unchanged `fails_closed` regression tests; the Windows-only originals still correctly report as skipped (not run, not failed).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Remaining macOS-only companion tests for service_host.py</name>
  <files>tests/test_service_host.py</files>
  <behavior>
    - Test (companion to line 97, `test_published_descriptor_carries_the_restricted_acl`): a descriptor written via `write_descriptor` on POSIX has mode `0o600` after publish.
    - Test (companion to line 112, `test_published_descriptor_is_owned_by_the_operator`): the published descriptor's `st_uid` equals `os.getuid()`.
    - Test (companion to line 134, `test_creation_sddl_explicitly_sets_owner_independent_of_default_token`): `create_protected_exclusive` sets the owner explicitly at creation (not inherited from an ambient umask) — assert the mode is `0o600` even when the process umask is permissive (e.g. `os.umask(0)` inside the test).
    - Test (companion to line 150, `test_verification_refuses_a_descriptor_owned_by_someone_else`): `verify_restricted_acl` returns `False` when the target's `st_uid` does not match `os.getuid()` (simulate via mocking `os.stat` or `os.getuid()`, not via actually chowning to another user).
    - Test (companion to line 225, `test_protected_birth_denies_every_second_handle_and_survives_close`): a second `create_protected_exclusive` call against the same path raises `FileExistsError` (the `O_EXCL` guarantee), and no residue is left after the first handle's owning code path closes and errors.
  </behavior>
  <action>
Add these 5 macOS-only companion test methods to `ServiceHostUnitTests` in `tests/test_service_host.py`, each gated with the same `if os.name == "nt": raise unittest.SkipTest("POSIX permissions are macOS/Linux only")` pattern as Task 1's new test, each named with a `_on_posix` or `_posix` suffix mirroring its Windows-only sibling's name so the pairing is discoverable by name alone (e.g. `test_published_descriptor_carries_the_restricted_acl_on_posix`). Reuse the POSIX implementation added in Task 1 — this task adds tests only, no further `engine/service_host.py` changes.
  </action>
  <verify>
    <automated>python -m unittest tests.test_service_host.ServiceHostUnitTests -v 2>&1 | grep -c "^ok\|ok$"</automated>
  </verify>
  <done>6 total macOS-only companion tests exist and pass on this machine (the one from Task 1 plus these 5), matching every row in RESEARCH.md's Runtime State Inventory for `tests/test_service_host.py`'s ACL/ownership tests; their Windows-only originals are unmodified and still correctly skip on POSIX.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Explicit disposition for tests blocked on earlier-phase work</name>
  <files>tests/test_claude_pipe_lifecycle.py, tests/test_process_lifetime.py, tests/test_startup_progress.py, tests/test_startup_readiness.py</files>
  <action>
For `tests/test_claude_pipe_lifecycle.py`'s two Windows-only tests (`test_idle_watchdog_ends_launcher_and_child_then_returns_queued_mail` at line 216, `test_expiry_releases_reader_even_when_a_child_keeps_the_pipe_open` at line 243, both currently `@unittest.skipUnless(os.name == "nt", ...)`): add a macOS-only companion method for each, porting the same assertions to POSIX process semantics (no `.cmd` wrapper — spawn the scripted `_CHILD` fixture directly; check process-group liveness instead of `windows_alive`). Mark each new method `@unittest.expectedFailure` with a leading comment naming the exact blocker: "Blocked on Phase 2 PROC-03 — engine/backend/orgtree/codexrun.py and antigravityrun.py's process-tree kill is missing `start_new_session` (see Plan 05-01 Task 2's pre-flight check for the automated tripwire); this test is expected to start passing once that fix lands, at which point remove `@unittest.expectedFailure`." This is a documented, source-cited red state — not a silent skip and not a false pass.

For `tests/test_process_lifetime.py`, `tests/test_startup_progress.py`, `tests/test_startup_readiness.py`: leave their existing Windows-only-gated guardian tests as-is (writing a real macOS test here requires a POSIX process-tree guardian design that does not exist anywhere in the codebase yet — `engine/process_lifetime.py::arm_process_lifetime` unconditionally raises `RuntimeError` on non-`nt` per this phase's Plan 05-01 objective). Strengthen each file's Windows-only skip/gate reason string to explicitly name the blocker: append ", no macOS counterpart yet — blocked on a POSIX process-tree guardian design (tracked in Plan 05-01, not owned by any current Phase 1-4 requirement)" to the existing skip reason text, so a future reader of test output sees the reason without needing to find this PLAN.md.
  </action>
  <verify>
    <automated>python -m unittest tests.test_claude_pipe_lifecycle -v 2>&1 | grep -c "expected failure"</automated>
  </verify>
  <done>The 2 new `test_claude_pipe_lifecycle.py` macOS companions exist, run (not skip) on this POSIX machine, and report as expected failures naming the exact Phase 2 blocker; the 3 guardian-blocked files' skip-reason strings now name the blocker explicitly instead of a bare generic reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem -> descriptor file (engine-attach.json) | Carries a bearer token; any other local account able to read it gains desktop-engine access |
| engine process -> descriptor write path | The only writer; must never leave a partially-written or wrongly-permissioned file visible to a reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | engine/service_host.py descriptor file (bearer token) | high | mitigate | POSIX `os.open(O_CREAT\|O_EXCL\|O_WRONLY, 0o600)` at creation + `os.stat().st_mode`/`st_uid` verification before publish, fail-closed (raise) on any unverified state — mirrors the existing Windows SDDL/icacls contract exactly (Task 1) |
| T-05-06 | Tampering | create_protected_exclusive's O_EXCL guarantee | medium | mitigate | Atomic `O_EXCL` open prevents a second writer/reader from acquiring a pre-restriction handle; verified via `test_protected_birth_denies_every_second_handle_and_survives_close`'s POSIX companion (Task 2) |
| T-05-07 | Repudiation | Test-suite silently skipping Windows-only tests with no macOS trace | medium | mitigate | Every blocked test's skip/failure reason string now names the exact blocking dependency, so `npm test`/`python -m unittest` output itself documents the gap (Task 3) |

## ASVS Category

| ASVS Category | Applies | Standard Control |
|---------------|---------|-------------------|
| V4 Access Control | yes | macOS companions to test_service_host.py's ACL/ownership tests are an access-control verification: owner-only file permissions replacing NTFS DACLs |

</threat_model>

<verification>
`python -m unittest tests.test_service_host -v` passes in full on this machine today (Windows-only tests correctly skip, all new POSIX tests pass). `python -m unittest tests.test_claude_pipe_lifecycle -v` shows the 2 new companions as expected failures with the Phase 2 blocker named in the failure output. `grep` over `tests/test_process_lifetime.py`, `test_startup_progress.py`, `test_startup_readiness.py` shows the strengthened skip-reason text.
</verification>

<success_criteria>
- `engine/service_host.py` has a real POSIX descriptor-protection implementation, not a no-op.
- 6 macOS-only tests exist in `tests/test_service_host.py` and pass today.
- 2 macOS-only tests exist in `tests/test_claude_pipe_lifecycle.py`, documented as expected-fail pending Phase 2.
- Every remaining Windows-only skip (guardian tests, NSIS/installer tests) has an explicit, source-cited disposition — either in its own skip-reason string or in this PLAN.md's recorded scope decision.
</success_criteria>

<output>
Create `.planning/phases/05-end-to-end-verification-test-coverage/05-02-SUMMARY.md` when done
</output>
