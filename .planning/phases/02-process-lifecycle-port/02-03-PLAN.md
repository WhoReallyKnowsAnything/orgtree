---
phase: 02-process-lifecycle-port
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_provider_path_resolution.py
autonomous: true
requirements: [PROC-01, PROC-02]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "antigravity_path() resolves via env override, private install location, then PATH fallback, in that precedence order, proven by mocked-path unit tests"
    - "supervisor.claude_install_state()/_claude_argv() resolve the Claude CLI via the identical env > pin > PATH precedence, proven by mocked-path unit tests"
    - "liveness.py's os.kill(pid, 0) POSIX branch is confirmed, via the existing test suite, to correctly distinguish a live process from a reaped/dead one on this macOS environment"
  artifacts:
    - tests/test_provider_path_resolution.py
  key_links:
    - "antigravity_path()'s env > install > PATH branches must each be exercised in isolation (one condition true at a time) to prove precedence ORDER, not merely that some path is returned"
    - "supervisor.claude_install_state(force=True) must bypass its 60s cache in every test case, or a mocked branch is silently served a stale cached result instead of being genuinely re-evaluated"
---

<objective>
Close PROC-01 and PROC-02 as the verification-only tasks RESEARCH.md found them to be: no new resolver or liveness code is needed (`codex_path()`, `antigravity_path()`, `supervisor.CLAUDE`/`_claude_argv()`/`claude_install_state()`, and `liveness.py::_observe_pid()`'s POSIX branch are all already correct), but two genuine test gaps exist per 02-VALIDATION.md's Wave 0 requirements: no mocked-path test exercises `antigravity_path()`'s or the Claude resolver's env > pin/install > PATH precedence order today.

Purpose: prove, not just assert, that PROC-01's three provider resolvers agree on precedence order (a silent divergence here would mean one provider picks up a stale/wrong binary while the others don't), and that PROC-02's liveness check matches real macOS process-state behavior already confirmed live this session.
Output: one new test file covering both resolvers' precedence order; a confirmation run of the existing liveness test; a checkpoint for the one manual-only verification 02-VALIDATION.md documents (Antigravity is not installed on this dev machine).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-lifecycle-port/02-CONTEXT.md
@.planning/phases/02-process-lifecycle-port/02-RESEARCH.md
@.planning/phases/02-process-lifecycle-port/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mocked-path resolution tests for antigravity_path() and supervisor.claude_install_state()</name>
  <files>tests/test_provider_path_resolution.py</files>
  <read_first>
    engine/backend/orgtree/providers.py (L447-458 `codex_path()` — the shape reference; L870-881 `_antigravity_install_path()`; L883-895 `antigravity_path()`)
    engine/backend/orgtree/supervisor.py (L320-326 the `CLAUDE`/`_PIN`/`_DATA` module-level resolution; L353-396 `claude_install_state(force=False)` — the re-probed, 60s-cached function that mirrors `CLAUDE`'s env > pin > PATH order; L528-533 `_claude_argv()`)
    tests/test_providers_force.py (L1-42 — the module's monkeypatch convention: `tempfile.TemporaryDirectory` + `os.environ['ORGTREE_DATA']` set before import + `sys.path.insert(.../engine/backend)` + `from orgtree import providers` + direct attribute reassignment restored in `tearDown`, in place of `unittest.mock.patch` decorators)
  </read_first>
  <behavior>
    - `antigravity_path()`, 3 cases: `ORGTREE_ANTIGRAVITY` env set -> returns `(env_value, "env")`; env unset + `_antigravity_install_path()` returns a path -> returns `(that_path, "install")`; env unset + install unset + `shutil.which("agy")` returns a path -> returns `(that_path, "path")`. A 4th case (all three absent) -> returns `(None, "")`.
    - `supervisor.claude_install_state(force=True)`, 3 cases mirroring the same precedence against `ORGTREE_CLAUDE` / `_PIN` existence / `shutil.which("claude")`: each case asserts the returned dict's `"source"` key is `"env"` / `"pin"` / `"path"` respectively, and `"installed"` is `True` when the mocked path is made to exist.
  </behavior>
  <action>
    In `tests/test_provider_path_resolution.py` (new file), following `tests/test_providers_force.py`'s import/setup convention, write two `unittest.TestCase` classes.

    `AntigravityPathResolutionTests`: for each of the three precedence branches (plus the all-absent case), monkeypatch `os.environ` (set/delete `ORGTREE_ANTIGRAVITY`), `providers._antigravity_install_path`, and `shutil.which` (module-level reassignment, restored in `tearDown`) so exactly one branch's condition is true per test method, call `providers.antigravity_path()` directly, and assert BOTH the returned path AND the returned source-tag match that branch — proving precedence order, not just a non-`None` result.

    `ClaudeInstallStateResolutionTests`: same three-branch shape (plus all-absent) against `sup.claude_install_state(force=True)` — `force=True` is required in every case to bypass the function's internal 60s cache (L353-396's cache-check at the top of the function would otherwise serve a stale prior result). Monkeypatch `os.environ` (`ORGTREE_CLAUDE`), `os.path.exists` (for the `_PIN` check), and `shutil.which`, asserting the returned dict's `"source"` and `"installed"` keys per branch.
  </action>
  <verify>
    <automated>python -m unittest tests.test_provider_path_resolution -v</automated>
    <fails_when>non-zero exit, or "FAILED" in the summary line, or any branch's asserted `source` tag does not match the branch under test (proves precedence ORDER was violated, not merely that some path came back)</fails_when>
    <human-check>
      <test>02-VALIDATION.md's Manual-Only Verifications table names one behavior this task's mocked tests cannot reach: install Antigravity on a real macOS machine, run the app, and use the agent picker to select the Antigravity provider</test>
      <expected>The agent picker resolves and launches the Antigravity provider via its real `~/.local/bin/agy` install, with no manual path override needed — matching what `antigravity_path()`'s mocked-path tests already prove logically</expected>
      <why_human>Antigravity is not installed on the dev machine used for this phase (confirmed via RESEARCH.md's live `which agy` probe: not found) — real-binary resolution can only be confirmed on a machine that has it installed; if unavailable, defer and note it as a known, non-blocking gap, since the resolution logic is already fully proven by this task's unit tests</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - tests/test_provider_path_resolution.py exists with at least 4 test methods for `antigravity_path()` (env / install / path / all-absent) and at least 4 for `supervisor.claude_install_state()` (env / pin / path / all-absent)
    - every `claude_install_state` test call passes `force=True`
    - each test asserts the branch-specific `source` tag, not merely that a path was returned
    - `python -m unittest tests.test_provider_path_resolution -v` exits 0
    - no changes made to `providers.py` or `supervisor.py` (verification-only, per RESEARCH.md's Critical Finding 1: no new resolver code needed)
  </acceptance_criteria>
  <done>`antigravity_path()` and `supervisor.claude_install_state()`/`_claude_argv()` are proven, by mocked-path unit test, to resolve via the same env > pin/install > PATH precedence order `codex_path()` already uses.</done>
  <reversibility rating="reversible">New test file only; no production code touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Verify liveness.py's os.kill(pid, 0) POSIX branch against real macOS behavior</name>
  <files>engine/backend/orgtree/liveness.py, tests/test_provider_attempt_and_liveness.py</files>
  <read_first>
    engine/backend/orgtree/liveness.py (L159-215 `_observe_pid()` — the POSIX branch at L190-198: `os.kill(pid, 0)` -> `"still-active"`, `ProcessLookupError` -> `"no-such-process"`, `PermissionError` -> `"access-denied"`, other `OSError` -> `"probe-error"`)
    tests/test_provider_attempt_and_liveness.py (L289-344 `LivenessRecord` class, specifically `test_3e_a_live_process_is_alive_and_a_reaped_one_is_dead` at L328-344, and the `test_3f_*` probe-failure tests at L348-406)
    .planning/phases/02-process-lifecycle-port/02-RESEARCH.md (the "Live-verified macOS process-lifecycle behavior" Code Examples, executed this session on Darwin 27.2.0 arm64: alive/reaped/zombie/`killpg` probes)
  </read_first>
  <action>
    No production code change: `liveness.py`'s POSIX branch already matches the live-verified macOS behavior recorded in RESEARCH.md this session. Run `test_3e_a_live_process_is_alive_and_a_reaped_one_is_dead` (and the sibling `test_3f_*` probe-failure tests in the same file) to reconfirm on this machine. In the plan's SUMMARY, record the documented zombie hazard (an exited-but-unreaped child still answers `os.kill(pid, 0)` as `"still-active"` — RESEARCH.md Pitfall 1, independently confirmed live this session) as a pre-existing, accepted characteristic of this POSIX branch, not a regression or a gap PROC-02 asks to close — the requirement only asks the branch to match real macOS behavior, which it does.
  </action>
  <verify>
    <automated>python -m unittest tests.test_provider_attempt_and_liveness -v</automated>
    <fails_when>non-zero exit, or "FAILED" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_provider_attempt_and_liveness -v` exits 0, including `test_3e_a_live_process_is_alive_and_a_reaped_one_is_dead`
    - no changes made to `liveness.py`
    - SUMMARY documents the zombie-hazard finding as accepted/pre-existing, citing RESEARCH.md's live-verified Code Examples
  </acceptance_criteria>
  <done>liveness.py's os.kill(pid, 0) POSIX branch is confirmed correct against real macOS process states (alive, reaped, zombie) via the existing test suite; no code change needed for PROC-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment variable → executed binary | `ORGTREE_CODEX` / `ORGTREE_ANTIGRAVITY` / `ORGTREE_CLAUDE` let a local env var redirect the engine to spawn an arbitrary binary under a provider's name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Spoofing | `codex_path()` / `antigravity_path()` / `supervisor.claude_install_state()` env-override branch | low | accept | Pre-existing, documented trust design (`supervisor.py`'s own docstring: the env override is "taken on faith as the path to use but still checked for existence"), unchanged by this phase; Task 1's tests assert existence-checking behavior only, not a trust-model change |
| T-02-08 | Information Disclosure | `liveness.py::_observe_pid()` zombie hazard | low | accept | Confirmed via live macOS probe this session (RESEARCH.md); a zombie briefly reports `"still-active"`, which could delay a caller treating a turn as dead by one poll cycle — no privileged action is taken on a stale "alive" read, and this is pre-existing, unchanged behavior |

No new package installs this phase — Package Legitimacy Gate not applicable (per RESEARCH.md's Package Legitimacy Audit: "Not applicable... zero external packages").

</threat_model>

<verification>
- `python -m unittest tests.test_provider_path_resolution -v`
- `python -m unittest tests.test_provider_attempt_and_liveness -v`
- `python -m unittest discover -s tests -p 'test_*.py'` — confirms no regression
</verification>

<success_criteria>
- Mocked-path tests prove `antigravity_path()` and `supervisor.claude_install_state()` share `codex_path()`'s env > pin/install > PATH precedence order
- Existing liveness test suite reconfirmed green on this macOS environment
- The one documented manual-only verification (Antigravity real-CLI resolution) is surfaced via Task 1's `<human-check>` for end-of-phase UAT, not silently skipped
- No production code changed in this plan
</success_criteria>

<output>
Create `.planning/phases/02-process-lifecycle-port/02-03-SUMMARY.md` when done
</output>
